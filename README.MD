Quick start (local)

Install Rust: https://www.rust-lang.org/tools/install
Clone and install dependencies:
git clone git@github.com:prjktcode/fhe-poker.git
cd fhe-poker
Build:
cargo build --workspace
Generate keys (example):
bash scripts/gen_keys.sh
Create a proof (example CLI described in zk/prover/README.md)
