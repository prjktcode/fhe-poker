{
  "name": "private-poker",
  "version": "1.0.0",
  "description": "Fully on-chain Texas Hold'em with FHE (Fhenix) and Privara",
  "scripts": {
    "test": "node test/poker.test.js",
    "test:verbose": "node test/poker.test.js --verbose",
    "compile": "echo 'Use forge build for Solidity contracts'",
    "deploy": "node scripts/deploy.js"
  },
  "keywords": ["poker", "fhenix", "privara", "fhe", "privacy", "blockchain"],
  "license": "MIT",
  "devDependencies": {
    "assert": "^2.1.0"
  }
}
